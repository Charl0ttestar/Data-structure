#include<stdio.h>  

/* 拿硬幣遊戲：
   雙方輪流拿走若干硬幣(最少1枚),直到把硬幣取完為止,誰拿到最後一個硬幣,誰就算勝利。
   此遊戲若知道硬幣總數,先拿的人贏面較大,假設共有N個硬幣,一次最多拿K個,則先拿的人只要先拿N%(K+1)枚
   ，就一定有辦法穩贏，如果N%(K+1)=0則可能會輸
*/

int main(){  
  
  int N,K,i;  
  /* 硬幣總數為N, 一次最多拿K個 */
  scanf("%d%d",&N,&K);  
  
  /* 算出先拿的人要拿幾枚硬幣才能穩贏 */
  for(i=K;i>0;i--){  
    K = N%(i+1);  
    if(K!=0){  
        printf("%d\n",K);  
        return 0;  
    }  
  }  
  /* 若無法取勝,輸出0 */
  printf("%d\n",0);  
  
  return 0;  
}  
